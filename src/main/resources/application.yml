server:
  port: 8085
spring:
  application:
    name: msa-config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:config/api-gateway/
            - classpath:config/document/