server:
  port: 25010

spring:
  application:
    name: api-gateway
  r2dbc:
    url: r2dbc:postgresql://194.233.91.140:2017/api_gateway_db
    username: postgres
    password: CAMBO@@2026

  data:
    redis:
      host: 194.233.91.140
      port: 16379
      timeout: 30000
      user: default
      #      password: CF@2025
      ssl:
        enabled: false

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://key-aim.cambofreelance.com/realms/cambofreelance.dev

keycloak:
  url: https://key-aim.cambofreelance.com
  realm: cambofreelance.dev
  audience-client-id: KPS_GAME
  audience-client-secret: yCygkPDxcPp3qtH0lGtBHvFhXKYBxTm1
  admin-cli-client-id: authentication-client
  admin-cli-client-secret: wJzYfm093CKXwCmzScq1Qm6IAVNCcoLz
  enable: Y

logging:
  enable: true
  includeRequest: true
  includeRequestBody: true
  maskLogKey: maskingKey001,maskingkey001,masking-key-002 # its key is sensitive
  ignoreDefaultMaskingLogKey: ignoreMaskingKey001,ignoreMasking-key-002
  includeRequestHeader: true
  maxRequestBody: 1000 # its value is byte
  ignoreAntMatches: /abc,/zte-ba/**,/hiding-log # this value just example
  includeResponse: true
  includeResponseHeader: true
  maskResponseHeaderKey: maskingKey001,maskingkey001,masking-key-002 # its key isn't sensitive
  includeResponseBody: true
  maxResponseBody: 200 # its value is byte, the log size response
  pattern:
    level: "%5p [${spring.application.name},%X{traceId},%X{spanId}]"
    console: "[%d] [%t] %-5level [${spring.application.name},%X{traceId},%X{spanId},%X{activityid}] %logger{36} - %msg%n"

otel:
  exporter:
    otlp:
      endpoint: http://alloy:4317
      protocol: grpc
  logs:
    exporter: none
  metrics:
    exporter: none
  traces:
    sampler:
      probability: 1.0

management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info,metrics,prometheus
  endpoint:
    Prometheus:
      access: read_only

authentication:
  jwtExpiration: 3600000 # 1 hour
  jwtRefreshExpiration: 2592000000 # 30 days in ms
  jwtSecret: b0aff574-a86e-4714-8108-63675311a839

storage:
  redis:
    key-response-code: key-response-code
    key-api-route: key-api-route

aes-cipher:
  gcm:
    key: UsLHzxoYLfUZ6sFbPs906dWmf3tulwJs/MfMAvT8SLk=
    algorithm: AES/GCM/NoPadding
    iv: ZGRFq4ZfB20=
    tag-size: 128
